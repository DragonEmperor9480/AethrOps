package cloudwatch

import (
	"fmt"
)

// LiveTailLambdaLogs will be reimplemented later
func LiveTailLambdaLogs() {
	fmt.Println("Live Tail Lambda Logs is not yet available in TUI mode.")
	fmt.Println("Please use the web UI to stream Lambda logs.")
}
